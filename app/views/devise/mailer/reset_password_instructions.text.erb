<%= @resource.email %> 様

パスワード再発行のご依頼を受け付けました。

こちらのリンクからパスワードの再発行を行ってください。

<%= edit_password_url(@resource, reset_password_token: @token) %>

※このメールに心当たりがない場合は、このメールを無視してください。
※上記のリンクからパスワードを変更するまで、パスワードは変更されません。
